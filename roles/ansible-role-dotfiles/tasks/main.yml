---

- name: Install needed packages (git, vim)
  package:
    name:
      - git
      - vim
    state: present
  become: true

- name: Install dotfiles
  become: true
  include: dotfiles.yml
  become_user: "{{ ansible_user_id }}"

- name: Install / Update VIM plugins
  include: vim.yml
